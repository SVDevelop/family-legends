import { AttachImagesPipe } from './attach-images.pipe';

describe('AttachImagesPipe', () => {
  it('create an instance', () => {
    const pipe = new AttachImagesPipe();
    expect(pipe).toBeTruthy();
  });
});
