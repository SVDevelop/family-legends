import { AttachImagePipe } from './attach-image.pipe';

describe('AttachImagePipe', () => {
  it('create an instance', () => {
    const pipe = new AttachImagePipe();
    expect(pipe).toBeTruthy();
  });
});
